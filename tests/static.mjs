import{readFileSync}from'node:fs';import assert from'node:assert/strict';const d=readFileSync('Dockerfile','utf8'),e=readFileSync('entrypoint.sh','utf8'),c=readFileSync('Caddyfile','utf8');assert.match(d,/mantisbt:2\.28\.4@sha256:[a-f0-9]{64}/);assert.match(d,/caddy:2\.10\.2-alpine@sha256:[a-f0-9]{64}/);assert.doesNotMatch(d,/:latest/);assert.match(e,/installed successfully/);assert.match(e,/user_set_password/);assert.match(e,/\.admin/);assert.match(c,/healthz/);console.log('static template checks passed');
